import { GetterTree } from 'vuex'
import { LolCompetitionBanState } from './state'
import { RootState } from '@/store'

export type LolCompetitionBanGetters<S = LolCompetitionBanState> = {
}

export const lolCompetitionBanGetters: GetterTree<LolCompetitionBanState, RootState> & LolCompetitionBanGetters = {
}
